# Copy the relevant section into server/.env and client/.env respectively.
# See CLAUDE.md Section 9 for the authoritative list.

# server/.env
DATABASE_URL=postgresql://user:password@localhost:5432/assetflow
JWT_SECRET=changeme
PORT=4000
CLIENT_URL=http://localhost:5173

# client/.env
VITE_API_URL=http://localhost:4000
